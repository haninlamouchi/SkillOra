<!DOCTYPE html>

<title>Edit MembreGroupe</title>

{% block body %}
    <h1>Edit MembreGroupe</h1>

    {{ include('membre_groupe/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_membre_groupe_index') }}">back to list</a>

    {{ include('membre_groupe/_delete_form.html.twig') }}
{% endblock %}
