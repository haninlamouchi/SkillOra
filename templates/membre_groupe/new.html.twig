<!DOCTYPE html>

<title>New MembreGroupe</title>

{% block body %}
    <h1>Create new MembreGroupe</h1>

    {{ include('membre_groupe/_form.html.twig') }}

    <a href="{{ path('app_membre_groupe_index') }}">back to list</a>
{% endblock %}
