<form method="post" action="{{ path('app_membre_groupe_delete', {'id': membre_groupe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ membre_groupe.id) }}">
    <button class="btn">Delete</button>
</form>
