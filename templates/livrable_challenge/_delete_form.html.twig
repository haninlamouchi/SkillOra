<form method="post" action="{{ path('app_livrable_challenge_delete', {'id': livrable_challenge.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livrable_challenge.id) }}">
    <button class="btn">Delete</button>
</form>
